language: javascript

tasks:
  dev:
    command: pnpm exec astro dev
  build:
    command: pnpm exec astro build
    inputs:
      - "src/**/*"
      - "public/**/*"
      - "astro.config.mjs"
  preview:
    command: pnpm exec astro preview
